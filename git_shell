git add .
git commit -m "update"
git push origin master
hi
update head branch

查看本地分支
git branch 
查看远程分支
git branch -r

改名
git branch -m mast master
同步远程的设置到本地
git fetch origin
git fetch --prune 从远程仓库获取最新的提交和分支信息，同时删除本地已经不存在于远程仓库的分支。

git branch -u origin/master master
git remote set-head origin -a